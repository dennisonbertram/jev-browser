# Required: the classifier that picks an operation and a target element.
TYPESAFE_API_KEY=
TYPESAFE_MODEL=jev-latest

# Required only when a task types text. Any OpenAI-compatible endpoint works.
TEXT_MODEL_BASE_URL=https://api.openai.com/v1
TEXT_MODEL_API_KEY=
TEXT_MODEL=gpt-4.1-nano
